#!/usr/bin/env bash

set -euo pipefail

recover_mount() {
local mount="$1"

```
echo "→ Recovering $(basename "$mount")"

sudo umount -lf "$mount" 2>/dev/null || true

sleep 1

if timeout 5 ls "$mount" >/dev/null 2>&1; then
    echo "  ✓ Online"
else
    echo "  ⚠ Waiting for automount"
fi

echo
```

}

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo " NAS Recovery"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo

if [[ $# -ne 1 ]]; then
echo "Usage: nas-recover <share|all>"
exit 1
fi

if [[ "$1" == "all" ]]; then
for mount in /mnt/nas/*; do
recover_mount "$mount"
done

```
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo " Recovery Complete"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━"
exit 0
```

fi

recover_mount "/mnt/nas/$1"

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo " Recovery Complete"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━"
